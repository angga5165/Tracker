{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Vanilla JS Expense Tracker with Supabase sync",
  "scripts": {
    "test": "jest",
    "test:run": "jest --runInBand"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "jest": "^29.7.0"
  }
}
